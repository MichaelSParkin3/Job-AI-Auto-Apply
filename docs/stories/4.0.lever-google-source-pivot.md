# Story 4.0 — Source Pivot: Google + Lever Apply

## Status
Draft — awaiting PO confirmation of defaults (2025-09-29)

## Story
As an operator,
I want the agent to discover recent Lever application pages via Google and drive their on-platform forms,
so that we can continue the review-first flow with a more uniform, reliable source than SimplyHired.

## Context Source
- Source: docs/prd/sprint-change-proposal-2025-09-29-lever-pivot.md
- Source: docs/prd/epic-4-review-gate-submission-integration.md (inserted Story 4.0)
- Source: docs/architecture/core-workflows.md (updated sequence)
- Source: docs/architecture/high-level-architecture.md (diagram updated)
- Source: docs/architecture/components.md (Lever provider notes)
- Snippets: docs/snippets/lever-and-google-snippets.md
- Prior Story Dependency: docs/stories/3.5.submission-summary-pre-submit-screenshot.md

## Acceptance Criteria
1. Discovery uses Google with query `site:jobs.lever.co/apply <terms>` and time window (`qdr:d|w|m|y`) to collect candidates; paginates via `start`.
2. Results are filtered to `jobs.lever.co` hosts; each result opens either the `/apply` URL directly or the job page then clicks the in-page "Apply for this job" to reach the form.
3. New Lever provider (`sites/lever/*`) detects common fields by label+control mapping, fills from profile, and uploads resume deterministically.
4. Pre-submit summary and screenshot are captured exactly as in Story 3.5, feeding the review queue unchanged.
5. Domain guardrails updated for Lever (`jobs.lever.co`, optional `api.lever.co`, widget host `newassets.hcaptcha.com` load-only); single-tab policy preserved.
6. CLI flag `--source lever-google` selects this provider; default can be toggled via profile.

## Tasks / Subtasks
- [ ] Discovery: build Google SERP URL builder (`tbs=qdr:*`, `start=10n`), filter to `jobs.lever.co`.
- [ ] Navigation: open `/apply` or click job page "Apply for this job" button.
- [ ] Mapping: implement label+control resolver for inputs, textareas, selects; prefer `data-qa` when present.
- [ ] Upload: deterministic resume upload; confirm success indicator/state.
- [ ] Summary: compile pre-submit summary + screenshot identical to Story 3.5.
- [ ] Guardrails: add Lever domains to profile allowlist; preserve one-tab policy.
- [ ] CLI: add `--source lever-google`; precedence: CLI → profile → global config.

## Testing
- Unit: URL builder (time window + pagination), domain filter, label→control mapping, file upload success detection.
- Integration (fixtures/dry-run): SERP parsing, Lever form fill/upload, summary capture, artifacts persisted.
- Guardrails: enforce allowed domains (`jobs.lever.co`, optional `api.lever.co`, load-only `newassets.hcaptcha.com`).
- Policy: remain review-only through Epic 4; ensure submit is never invoked in this story.

## File List (to be updated during implementation)
- sites/lever/lever_google_discovery.py
- sites/lever/selectors/*.json
- sites/lever/form_executor.py
- apps/cli/commands/apply.py (source flag)
- config/profile schema additions

## Dev Agent Record
- Agent Model Used: (tbd)
- Debug Log References: (tbd)
- Completion Notes: (tbd)
- Change Log: (tbd)
