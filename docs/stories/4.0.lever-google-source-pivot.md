# Story 4.0 — Source Pivot: Google + Lever Apply

## Status
Ready for QA — navigation, review artifacts, and guardrails/CLI wiring landed (2025-11-10)

## Story
As an operator,
I want the agent to discover recent Lever application pages via Google and drive their on-platform forms,
so that we can continue the review-first flow with a more uniform, reliable source than SimplyHired.

## Context Source
- Source: docs/prd/sprint-change-proposal-2025-09-29-lever-pivot.md
- Source: docs/prd/epic-4-review-gate-submission-integration.md (inserted Story 4.0)
- Source: docs/architecture/core-workflows.md (updated sequence)
- Source: docs/architecture/high-level-architecture.md (diagram updated)
- Source: docs/architecture/components.md (Lever provider notes)
- Snippets: docs/snippets/lever-and-google-snippets.md
- Prior Story Dependency: docs/stories/3.5.submission-summary-pre-submit-screenshot.md

## Acceptance Criteria
1. Discovery uses Google with query `site:jobs.lever.co/apply <terms>` and time window (`qdr:d|w|m|y`) to collect candidates; paginates via `start`.
2. Results are filtered to `jobs.lever.co` hosts; each result opens either the `/apply` URL directly or the job page then clicks the in-page "Apply for this job" to reach the form.
3. New Lever provider (`sites/lever/*`) detects common fields by label+control mapping, fills from profile, and uploads resume deterministically.
4. Pre-submit summary and screenshot are captured exactly as in Story 3.5, feeding the review queue unchanged.
5. Domain guardrails updated for Lever (`jobs.lever.co`, optional `api.lever.co`, widget host `newassets.hcaptcha.com` load-only); single-tab policy preserved.
6. CLI flag `--source lever-google` selects this provider; default can be toggled via profile.

## Tasks / Subtasks
- [x] Discovery: build Google SERP URL builder (`tbs=qdr:*`, `start=10n`), filter to `jobs.lever.co`.
- [x] Navigation: open `/apply` or click job page "Apply for this job" button.
- [x] Mapping: implement label+control resolver for inputs, textareas, selects; prefer `data-qa` when present.
- [x] Upload: deterministic resume upload; confirm success indicator/state.
- [x] Summary: compile pre-submit summary + screenshot identical to Story 3.5.
- [x] Guardrails: add Lever domains to profile allowlist; preserve one-tab policy.
- [x] CLI: add `--source lever-google`; precedence: CLI → profile → global config.

### Progress Notes
- Added `LeverGoogleDiscovery` with query builder, pagination, and SERP parsing helpers plus unit coverage.
- Created `LeverFormPlanner`/`LeverFormExecutor` pipeline backed by selector JSON; detects fields by label + `data-qa` selectors.
- Added resume upload success detector to surface deterministic widget state for guardrails.
- Implemented `LeverNavigator` with safe-click fallback to open Lever apply forms from SERP/job results and unit coverage for navigation paths.
- Added `LeverSummaryBuilder` to emit Story 3.5-compatible summaries and capture review screenshots via Browser-Use.
- Updated CLI guardrails/defaults and introduced `apply plan --source lever-google` to honor CLI → profile → global precedence.

## Testing
- Unit: URL builder (time window + pagination), domain filter, label→control mapping, file upload success detection, navigation helper coverage, summary builder capture, CLI planning precedence.
- Integration (fixtures/dry-run): SERP parsing, Lever form fill/upload, summary capture, artifacts persisted.
- Guardrails: enforce allowed domains (`jobs.lever.co`, optional `api.lever.co`, load-only `newassets.hcaptcha.com`) plus CLI defaults for lever-google discovery.
- Policy: remain review-only through Epic 4; ensure submit is never invoked in this story.

## File List (to be updated during implementation)
- sites/lever/lever_google_discovery.py
- sites/lever/selectors/*.json
- sites/lever/form_executor.py
- sites/lever/navigation.py
- sites/lever/summary.py
- sites/lever/tests/test_lever_google_discovery.py
- sites/lever/tests/test_form_planner.py
- sites/lever/tests/test_navigation.py
- sites/lever/tests/test_summary.py
- apps/cli/commands/apply.py (source flag)
- config/profile schema additions

## Dev Agent Record
- Agent Model Used: gpt-5-codex (OpenAI)
- Debug Log References: Local pytest run (`pytest sites/lever/tests -q`).
- Completion Notes: Navigation, summary capture, guardrails, and CLI source selection implemented; lever-google provider ready for dry-run QA.
- Change Log: 2025-10-27 — Added Lever discovery planner, form planner/executor, and unit tests validating query encoding, SERP filtering, label mapping, and resume success detection.
- Change Log: 2025-11-10 — Added navigation + summary modules, expanded guardrails defaults, introduced `apply plan --source lever-google`, and extended unit coverage.

## QA Findings (2025-11-10)
- Gate: PASS → docs/qa/gates/4.0-lever-google-source-pivot.yml
- Tests Executed: `pytest sites/lever/tests -q` (discovery planner, navigation, summary capture, CLI planning).
- Coverage Summary: AC1–AC6 now have unit-level coverage; guardrails/CLI defaults exercise precedence and review artifacts persist before submission.
- Key Notes:
  - Navigation helper surfaces deterministic apply transitions with safe-click fallback when anchors fail.
  - Summary builder reuses Story 3.5 payload shape to keep preview UI consistent and captures screenshots through Browser-Use.
  - CLI `apply plan --source lever-google` honors CLI → profile → global precedence and ships Lever guardrail defaults.
