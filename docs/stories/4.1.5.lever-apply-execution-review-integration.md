# Story 4.1.5 — Lever Apply Execution & Review Integration

## Status
Draft

## Story
**As an** operator,
**I want** the CLI to drive Lever apply forms end-to-end and feed the review queue,
**so that** dry-run sessions capture complete artifacts for human approval before submission.

## Acceptance Criteria
1. `python app.py apply run --source lever-google` consumes `LeverGoogleDiscovery.plan` output (plan file path or freshly generated) and iterates each candidate, launching Browser-Use to reach the Lever apply form using `LeverNavigator` fallbacks.
2. For each discovered candidate, the CLI generates a `LeverFormPlan`, executes `LeverFormExecutor`, uploads the resume via `ResumeUploader`, and persists artifacts under `runs/<id>/lever/<candidateId>/` with redacted telemetry mirrored in `run.json`.
3. Each candidate is enqueued as an `ApplicationCandidate` (`discovered → planned → awaiting_decision`) with posting metadata, plan path, and resume status recorded in `queue.json` and exposed through `/api/queue/{runId}`.
4. Summary builder captures the pre-submit payload and screenshot for every candidate and appends them to the preview queue without triggering an actual Lever submission (review mode only, guardrails enforce `jobs.lever.co`, `api.lever.co`, `newassets.hcaptcha.com`).
5. Tests cover the new CLI path (unit + integration): mock Browser-Use controller to verify guardrail enforcement, queue persistence, summary artifacts, and resume upload telemetry; regression suite runs via `pytest sites/lever/tests -q`.

## Tasks / Subtasks
- [ ] Extend CLI run flow for Lever (AC: 1,2)
  - [ ] Update `apps/cli/main.py` to add `apply run --source lever-google` option that loads plan payloads and iterates Lever candidates before launching Browser-Use. [Source: architecture/components.md#CLI Orchestrator]
  - [ ] Wire `LeverNavigator`, `LeverFormPlanner`, `LeverFormExecutor`, and `ResumeUploader` into the new command, ensuring guardrail configuration and pacing overrides respect profile defaults. [Source: architecture/components.md#Browserâ€‘Use Controller]
  - [ ] Persist per-candidate artifacts under `runs/<id>/lever/<candidateId>/` including HTML snapshots and telemetry. [Source: architecture/backend-architecture.md#Database Architecture (File Repository)]
- [ ] Queue + run persistence integration (AC: 2,3)
  - [ ] Record `LeverFormPlan`, fill summary, and resume results into `run.json` using existing run-store helpers. [Source: architecture/data-models.md#Run]
  - [ ] Enqueue `ApplicationCandidate` entries and update `queue.json` state transitions (`discovered → planned → awaiting_decision`). [Source: architecture/data-models.md#ReviewQueueSnapshot]
  - [ ] Surface queue updates via `/api/queue/{runId}` so the preview UI reflects new candidates. [Source: architecture/api-specification-rest.md#API Specification (REST)]
- [ ] Preview artifacts & guardrails (AC: 4)
  - [ ] Invoke `LeverSummaryBuilder` to render pre-submit summaries and screenshots; attach outputs to queue payloads for the preview UI. [Source: architecture/core-workflows.md#Core Workflows]
  - [ ] Ensure guardrail configuration enforces Lever domain allowlist and single-tab policy during Browser-Use execution. [Source: architecture/components.md#Browserâ€‘Use Controller]
  - [ ] Keep the flow in `review` mode only—no final submission actions despite successful form fill. [Source: architecture/core-workflows.md#Run Modes & Decision Flow]
- [ ] Test coverage (AC: 5)
  - [ ] Add pytest coverage for the new CLI command path with stubbed Browser-Use controller verifying form planning, execution, summary capture, and queue persistence. [Source: architecture/testing-strategy.md#Testing Strategy]
  - [ ] Update existing `sites/lever/tests` to cover resume upload telemetry and guardrail enforcement for Lever runs. [Source: architecture/testing-strategy.md#Testing Strategy]

## Dev Notes
- **Previous Story Insights**
  - Story 4.0 delivered `LeverGoogleDiscovery`, `LeverNavigator`, form planning/execution helpers, resume upload success detector, guardrail defaults, and CLI planning flag `apply plan --source lever-google`; reuse these modules and assumptions. [Source: stories/4.0.lever-google-source-pivot.md#Progress Notes]
- **Data Models**
  - `RunRecord` must track `mode`, artifacts, and queue snapshots; decisions array holds audit trail. [Source: architecture/data-models.md#Run]
  - `ReviewQueueSnapshot` captures pending/escalated candidates and their states; ensure updates follow allowed transitions and include `lastUpdated`. [Source: architecture/data-models.md#ReviewQueueSnapshot]
  - `SubmissionDecision` schema (candidateId, outcome, mode, confidence, rationale, timestamp) underpins queue API payloads. [Source: architecture/data-models.md#SubmissionDecision]
- **API Specifications**
  - `/api/queue/{runId}` GET returns queue snapshot; `/api/queue/{runId}/decision` accepts decisions for human review—new CLI integration must populate data consumed by these endpoints. [Source: architecture/api-specification-rest.md#API Specification (REST)]
- **Component Specifications**
  - CLI orchestrator coordinates config/profile loading, Browser-Use controller, and artifact persistence; new command should live alongside existing `apply` subcommands. [Source: architecture/components.md#CLI Orchestrator]
  - Browser-Use controller provides `open_url`, `wait_for_idle`, `safe_click`, `capture_review_artifacts`, and guardrail enforcement utilities reused for Lever flows. [Source: architecture/components.md#Browserâ€‘Use Controller]
  - Frontend preview stack relies on queue snapshots and summary artifacts (screenshot + HTML) rendered by `LeverSummaryBuilder`. [Source: architecture/frontend-architecture.md#State Management Architecture]
- **File Locations**
  - CLI modifications: `apps/cli/main.py`, potential helpers in `apps/cli/run_store.py` or related modules. [Source: architecture/unified-project-structure.md#Unified Project Structure]
  - Lever provider modules live under `sites/lever/`; add tests under `sites/lever/tests/`. [Source: architecture/unified-project-structure.md#Unified Project Structure]
  - Preview server endpoints in `apps/preview/`; ensure queue persistence aligns with file store conventions. [Source: architecture/unified-project-structure.md#Unified Project Structure]
- **Testing Requirements**
  - Unit tests with pytest for CLI orchestration and queue persistence; Vitest/RTL reserved for UI but unnecessary unless new components introduced. [Source: architecture/testing-strategy.md#Testing Strategy]
  - Integration tests should use FastAPI TestClient or Browser-Use stubs to verify end-to-end artifact capture without hitting live Lever. [Source: architecture/testing-strategy.md#Testing Strategy]
- **Technical Constraints**
  - Continue using dry-run review mode; do not submit Lever forms. [Source: architecture/core-workflows.md#Run Modes & Decision Flow]
  - Guardrails restrict navigation to Lever domains and enforce single-tab control during automation. [Source: architecture/components.md#Browserâ€‘Use Controller]
  - Browser-Use pacing defaults (wait jitter, think time) must respect profile overrides. [Source: stories/4.0.lever-google-source-pivot.md#Acceptance Criteria]

### Testing
- pytest for CLI orchestration and Lever provider integration (`sites/lever/tests`). [Source: architecture/testing-strategy.md#Testing Strategy]
- Mock Browser-Use controller in tests to validate guardrail and artifact outputs without live automation. [Source: architecture/testing-strategy.md#Testing Strategy]

## Change Log
| Date | Version | Description | Author |
| --- | --- | --- | --- |
| 2025-09-29 | 0.1 | Initial draft created for Story 4.1.5 | Bob (Scrum Master) |

## Dev Agent Record
- **Agent Model Used**
- **Debug Log References**
- **Completion Notes**
- **File List**

## QA Results
